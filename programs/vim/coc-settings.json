{
  "cSpell.enabled": false,
  "cSpell.userWords": [
    "CPEs",
    "Serializers",
    "distro",
    "elems",
    "godoc",
    "monokai",
    "pino",
    "recognise",
    "resliced",
    "serialisers",
    "structs",
    "unbuffered",
    "vuln",
    "vulns"
  ],
  "typescript.format.enabled": true,
  "coc.preferences.bracketEnterImprove": true,
  "coc.preferences.formatOnSave": true,
  "prettier.disableLanguages": [
    // I'm surprised this works??
    // But it seems to also not touch yaml anymore (which might be fine too.)
    "helm",
    "shell"
  ],
  "yaml.schemaStore.enable": true,
  "yaml.schemas": {
    "kubernetes": "*.yaml",
    "http://json.schemastore.org/github-workflow": ".github/workflows/*",
    "http://json.schemastore.org/github-action": ".github/action.{yml,yaml}",
    "http://json.schemastore.org/prettierrc": ".prettierrc.{yml,yaml}",
    "http://json.schemastore.org/kustomization": "kustomization.{yml,yaml}",
    "http://json.schemastore.org/chart": "Chart.{yml,yaml}",
    "https://raw.githubusercontent.com/compose-spec/compose-spec/master/schema/compose-spec.json": "*docker-compose*.{yml,yaml}"
  },
  // gopls needs to be *compiled* with a specific version of Go to support it's features.
  // We're installing gopls through unstable, which means it's built with the Go version
  // in unstable too. `coc-go` would build gopls with our locally installed version of Go,
  // which is from stable, ergo older.
  "go.goplsPath": "/Users/ramon/.nix-profile/bin/gopls",
  "go.goplsOptions": {
    "completeUnimported": true,
    "buildFlags": ["-tags", "integration,e2e"]
  },
  "go.tags": {
    "skipUnexported": true
  },
  "tslint.autoFixOnSave": true,
  "diagnostic-languageserver.filetypes": {
    // lint `sh` (includes `bash`) files
    "sh": "shellcheck"
  },
  "diagnostic-languageserver.formatFiletypes": {
    // format `sh` (includes `bash`) files using formatter defined below
    "sh": "shfmt"
  },
  "diagnostic-languageserver.formatters": {
    // define our formatter so that we can reference it from
    // `diagnostic-languageserver.formatFiletypes`
    "shfmt": {
      "command": "shfmt"
    }
  },
  "languageserver": {
    "terraform": {
      "command": "terraform-ls",
      "args": ["serve"],
      "filetypes": ["terraform", "tf"],
      "initializationOptions": {},
      "settings": {}
    },
    "tiltfile": {
      "command": "tilt",
      "args": ["lsp", "start", "--verbose"],
      "filetypes": ["tiltfile"],
      "rootPatterns": [".git"]
    },
    "fish-lsp": {
      "command": "fish-lsp",
      "filetypes": ["fish"],
      "args": ["start"]
    },
    "helm": {
      "command": "helm_ls",
      "args": ["serve"],
      "filetypes": ["helm"],
      "rootPatterns": ["Chart.yaml"],
      "settings": {
        "helm-ls": {
          "valuesFiles": {
            "additionalValuesFilesGlobPattern": "*.yaml"
          }
        }
      }
    }
  }
}
